<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      RECEIVE_SMS observes alerts as they arrive. READ_SMS is asked for one job only: after
      an update, reading back the transaction alerts that arrived while a fault stopped them
      being recorded. An agent whose takings went missing cannot get them back any other way
      — the messages are on the phone and nowhere else. SEND_SMS is deliberately absent: the
      product observes and reconciles, it does not transact.
    -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!--
      Telephony is not required. On a device without it, automatic capture is simply
      unavailable and manual capture continues to work.
    -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:name=".ZaziApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zazi">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Exported because the platform itself delivers the broadcast. The android:permission
          attribute is the control that matters: it requires the sender to hold
          BROADCAST_SMS, which only the system has, so another application cannot inject a
          forged transaction message.
        -->
        <!--
          Fires once after this app is updated. An agent does not open Zazi to check on it;
          they open it when something looks wrong. Catching up at update time is what makes a
          fix reach the money without anyone being told to do anything.
        -->
        <receiver
            android:name=".sms.PackageReplacedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".sms.SmsBroadcastReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="500">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!--
          WorkManager's default initialiser is removed so the application can supply
          ZaziWorkerFactory. Without this, SyncWorker would be constructed reflectively with
          no dependencies and could not reach the sync engine.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
          Hands a downloaded statement to whatever the agent chooses — WhatsApp, email, Files —
          without making the app's storage readable to anything else. Only the statements folder
          in the cache is exposed, and only through a grant on each shared file.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.statements"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/statement_paths" />
        </provider>

    </application>

</manifest>
