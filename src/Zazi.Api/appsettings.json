{
  "//": [
    "Secrets are never committed. Supply them per-environment via environment variables,",
    "which override these values:",
    "  ConnectionStrings__DefaultConnection  PostgreSQL connection string.",
    "                                        Leave unset to run against an in-memory store.",
    "  Jwt__Key (or ZAZI_JWT_KEY)         Random secret, at least 32 bytes.",
    "                                        The API refuses to start without one outside Development.",
    "For local development use 'dotnet user-secrets' rather than editing this file."
  ],
  "ConnectionStrings": {
    "DefaultConnection": ""
  },
  "Jwt": {
    "Issuer": "zazi",
    "Audience": "zazi-clients",
    "AccessTokenMinutes": 60,
    "RefreshTokenDays": 14
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
